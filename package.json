{
  "name": "testing",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "pretest": "NODE_ENV=test npm explore web -- npm run build",
    "test": "NODE_ENV=test npx cucumber-js --no-strict",
    "posttest": "NODE_ENV=development npm explore web -- npm run config"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "puppeteer": "^1.17.0",
    "spa-react": "git+https://github.com/iskomt/frontendrepo.git"
  },
  "devDependencies": {
    "cucumber": "^5.1.0"
  }
}
